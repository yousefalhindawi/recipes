const Footer = () => {
  return (
    <div className="bg-success text-light fw-bold text-center py-3">
      <p>&copy; {new Date().getFullYear()} Recipe App</p>
    </div>
  );
};

export default Footer;
